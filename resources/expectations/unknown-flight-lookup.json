{
  "scenario_id": "unknown-flight-lookup",
  "expectation_version": "1.0.0",
  "valid_paths": [
    {
      "path_id": "path_unknown_flight",
      "name": "Unknown Flight Error Handling",
      "description": "Attempts status lookup for invalid flight ID and handles returned error gracefully.",
      "expected_actions": [
        {
          "node_id": "node_unknown_lookup",
          "label": "Unknown Flight Lookup",
          "selector": {
            "tool_name": "flight.get_status",
            "argument_constraints": [
              {
                "field_pointer": "/flight_id",
                "operator": "equals",
                "value": "XX999"
              }
            ]
          },
          "required": true
        }
      ]
    }
  ]
}
